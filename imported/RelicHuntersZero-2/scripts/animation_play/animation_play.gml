///animation_play('id',speed,type,priority,frames)
// an_loop = plays animation in a constant loop
// an_clamp = plays the animation once and then stops

if argument3 >= animation_priority && animation_current != argument0
{
    animation_index = 0;
    animation_speed = argument1;
    animation_frames = argument4;
    animation_current = argument0;
    animation_type = argument2;
    animation_priority = argument3;
}
